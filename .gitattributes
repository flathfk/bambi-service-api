# 텍스트 파일은 저장소에 LF로 통일 (팀원 OS 혼재 대비)
* text=auto eol=lf

# Windows 배치 파일은 CRLF
*.bat  text eol=crlf

# gradlew 는 Linux/Mac 에서 실행되므로 반드시 LF
gradlew text eol=lf

# 바이너리는 변환 금지 (wrapper jar 손상 방지)
*.jar  binary
