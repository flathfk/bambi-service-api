-- =============================================================
-- V8__user_profile.sql — 프로필 화면용 사용자 소개(bio)
-- -------------------------------------------------------------
-- 근거: 프로필 목업(profile-self/user.html) 헤더의 소개문. 07-31 메뉴 전면 연결 결정.
-- 앱 검증은 120자(목업 카운터 기준) — 컬럼은 여유를 두고 300.
-- 소유: 프로필 API 세트 = 우석.
--
-- 번호 조율(07-31): V6 = 소라 agent_context_version 예약, V7 = 영현 scraps 예약.
-- 병렬 작업이라 머지 순서가 번호 순서와 다를 수 있어 flyway out-of-order 를 켠다
-- (application.yml 참조). 배포된 앞 버전 수정 금지, 추가만.
-- =============================================================

ALTER TABLE service.users
    ADD COLUMN bio VARCHAR(300);

COMMENT ON COLUMN service.users.bio IS '프로필 소개문(앱 검증 120자). 프로필 화면 헤더에 노출.';
